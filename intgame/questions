Что будет, если используя несколько раз app.use(express.static('public')); на разные деректории там будут одинаковые файлы. На сайте написано, что они просматриваются в порядке объявления. Т.е. второй файл с таким же именем мы не сможем получить???
как работает next

Распросить про сокеты и как они там юзаются.
Trailers will only be emitted if chunked encoding is used for the response; почему только в chunked???

как сделать так, что бы не прописывать жесткие пути в форме.

почему не работает express req.on('data'). как до этого доступиться

как тестировать методы (приватные внутри модулей).

удобный запуск тестов.

vviital@vviital-K53SV:~/nodejs/project/intgame/test$ mocha httpPageSearcherTest.js 


  module httpPageSearcher test
    ✓ module httpPageSearcher exist
[Function]
[Function]
    ✓ Searcher return page (1022ms)


  2 passing (1s)

почему выводится [Function]